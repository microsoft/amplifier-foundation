============================= test session starts ==============================
platform linux -- Python 3.12.3, pytest-9.0.2, pluggy-1.6.0 -- /home/bkrabach/.local/bin/python
cachedir: .pytest_cache
rootdir: /home/bkrabach/dev/hw-model-performance/lanes/ux32-anchors-dry-refactor/amplifier-foundation
configfile: pyproject.toml
plugins: amplifier-core-1.6.0, timeout-2.4.0, pytest_httpserver-1.1.4, anyio-4.12.1, asyncio-1.3.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_fixture_loop_scope=function, asyncio_default_test_loop_scope=function
collecting ... collected 5 items

tests/test_anchors_bundles_dry.py::TestAmpDevIsAThinVariant::test_declares_no_session_tools_or_hooks FAILED [ 20%]
tests/test_anchors_bundles_dry.py::TestAmpDevIsAThinVariant::test_includes_anchors_by_full_url FAILED [ 40%]
tests/test_anchors_bundles_dry.py::TestNoParallelCopies::test_no_same_path_file_in_both_bundles FAILED [ 60%]
tests/test_anchors_bundles_dry.py::TestNoParallelCopies::test_exactly_one_copy_of_each_ecosystem_doc FAILED [ 80%]
tests/test_anchors_bundles_dry.py::TestAgentDescriptionsCarryNoExampleBlocks::test_no_example_or_commentary_in_any_agent_description FAILED [100%]

=================================== FAILURES ===================================
_______ TestAmpDevIsAThinVariant.test_declares_no_session_tools_or_hooks _______

self = <tests.test_anchors_bundles_dry.TestAmpDevIsAThinVariant object at 0xfa560c6f4230>

    def test_declares_no_session_tools_or_hooks(self) -> None:
        """The runtime is anchors'. Re-declaring it forks the mount plan.
    
        `session:`, `tools:` and `hooks:` are anchors' single source of truth.
        A duplicate block here is how the two bundles' orchestrator, context
        window, tool roster or hook set drift apart without anyone editing both.
        """
        frontmatter = _frontmatter(AMP_DEV_DIR / "bundle.md")
        declared = [k for k in ("session", "tools", "hooks") if k in frontmatter]
>       assert not declared, (
            f"bundles/anchors-amp-dev/bundle.md re-declares {declared}; these "
            "must come from the anchors include so there is exactly one source "
            "of truth for the runtime."
        )
E       AssertionError: bundles/anchors-amp-dev/bundle.md re-declares ['session', 'tools', 'hooks']; these must come from the anchors include so there is exactly one source of truth for the runtime.
E       assert not ['session', 'tools', 'hooks']

tests/test_anchors_bundles_dry.py:91: AssertionError
__________ TestAmpDevIsAThinVariant.test_includes_anchors_by_full_url __________

self = <tests.test_anchors_bundles_dry.TestAmpDevIsAThinVariant object at 0xfa560c6f4470>

    def test_includes_anchors_by_full_url(self) -> None:
        """The anchors include is a full URL with a #subdirectory= fragment."""
        includes = _frontmatter(AMP_DEV_DIR / "bundle.md").get("includes") or []
        sources = [
            entry.get("bundle") if isinstance(entry, dict) else entry
            for entry in includes
        ]
        matching = [s for s in sources if isinstance(s, str) and ANCHORS_INCLUDE_RE.match(s)]
>       assert matching, (
            "bundles/anchors-amp-dev/bundle.md must include anchors by full URL "
            "(git+https://github.com/microsoft/amplifier-foundation@main"
            "#subdirectory=bundles/anchors/bundle.md). "
            f"Found includes: {sources}"
        )
E       AssertionError: bundles/anchors-amp-dev/bundle.md must include anchors by full URL (git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=bundles/anchors/bundle.md). Found includes: ['git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=behaviors/streaming-ui.yaml', 'git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=behaviors/status-context.yaml', 'git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=behaviors/redaction.yaml', 'git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=behaviors/logging.yaml', 'git+https://github.com/microsoft/amplifier-bundle-amplifier-tester@main', 'git+https://github.com/microsoft/amplifier-bundle-context-intelligence@main#subdirectory=behaviors/context-intelligence-logging.yaml', 'git+https://github.com/microsoft/amplifier-bundle-context-intelligence@main#subdirectory=behaviors/context-intelligence-navigation.yaml']
E       assert []

tests/test_anchors_bundles_dry.py:105: AssertionError
_________ TestNoParallelCopies.test_no_same_path_file_in_both_bundles __________

self = <tests.test_anchors_bundles_dry.TestNoParallelCopies object at 0xfa560c6f4770>

    def test_no_same_path_file_in_both_bundles(self) -> None:
        """A same-path file in both trees is a fix that will land on one side.
    
        #327 edited the anchors-amp-dev copies of six agents and system.md and
        left the anchors originals behind; the CLI default bundle then shipped
        the un-fixed text for weeks. This test makes that state unreachable.
        """
        shared = _relative_files(ANCHORS_DIR) & _relative_files(AMP_DEV_DIR)
        offenders = sorted(shared - SHARED_PATH_ALLOWLIST)
>       assert not offenders, (
            "These paths exist under BOTH bundles/anchors/ and "
            f"bundles/anchors-amp-dev/: {offenders}. Only "
            f"{sorted(SHARED_PATH_ALLOWLIST)} may be duplicated -- everything "
            "else must be inherited via the anchors include."
        )
E       AssertionError: These paths exist under BOTH bundles/anchors/ and bundles/anchors-amp-dev/: ['agents/architect.md', 'agents/builder.md', 'agents/debugger.md', 'agents/explorer.md', 'agents/git-ops.md', 'agents/researcher.md', 'context/system.md']. Only ['README.md', 'bundle.md'] may be duplicated -- everything else must be inherited via the anchors include.
E       assert not ['agents/architect.md', 'agents/builder.md', 'agents/debugger.md', 'agents/explorer.md', 'agents/git-ops.md', 'agents/researcher.md', ...]

tests/test_anchors_bundles_dry.py:125: AssertionError
_______ TestNoParallelCopies.test_exactly_one_copy_of_each_ecosystem_doc _______

self = <tests.test_anchors_bundles_dry.TestNoParallelCopies object at 0xfa560c6f4a10>

    def test_exactly_one_copy_of_each_ecosystem_doc(self) -> None:
        """Each Amplifier-ecosystem doc exists exactly once on the shipped surface.
    
        Copies under `experiments/` are excluded and reported, not counted --
        see UNSHIPPED_TREES for why.
        """
        for name in ECOSYSTEM_DOCS:
            everywhere = sorted(
                str(p.relative_to(REPO_ROOT))
                for p in REPO_ROOT.rglob(name)
                if not set(p.relative_to(REPO_ROOT).parts) & {".git"}
            )
            unshipped = [
                c for c in everywhere if c.split("/", 1)[0] in UNSHIPPED_TREES
            ]
            shipped = [c for c in everywhere if c not in unshipped]
>           assert len(shipped) == 1, (
                f"Expected exactly one shipped copy of {name}, found "
                f"{len(shipped)}: {shipped}. Two copies drift; the canonical "
                "location is context/amplifier-dev/. "
                f"(Excluded, frozen and unshipped: {unshipped})"
            )
E           AssertionError: Expected exactly one shipped copy of ecosystem-map.md, found 2: ['bundles/anchors-amp-dev/context/amplifier-dev/ecosystem-map.md', 'context/amplifier-dev/ecosystem-map.md']. Two copies drift; the canonical location is context/amplifier-dev/. (Excluded, frozen and unshipped: ['experiments/behavioral-anchor-amplifier-dev/context/amplifier-dev/ecosystem-map.md'])
E           assert 2 == 1
E            +  where 2 = len(['bundles/anchors-amp-dev/context/amplifier-dev/ecosystem-map.md', 'context/amplifier-dev/ecosystem-map.md'])

tests/test_anchors_bundles_dry.py:148: AssertionError
_ TestAgentDescriptionsCarryNoExampleBlocks.test_no_example_or_commentary_in_any_agent_description _

self = <tests.test_anchors_bundles_dry.TestAgentDescriptionsCarryNoExampleBlocks object at 0xfa560c6f4cb0>

    def test_no_example_or_commentary_in_any_agent_description(self) -> None:
        """Extends #341's sweep, which covered the repo-root `agents/` only.
    
        `description-authoring-principles.md` V3 is 'no `<example>` blocks --
        not "at most 2", zero'. #341 (1b16a7d) applied that to the 16 root
        agents and stopped there, leaving the bundle agent trees as the repo's
        last violators. The policy is repo-wide, so the check is too.
        """
        offenders: list[str] = []
        for agent_file in _agent_files():
            description = (_frontmatter(agent_file).get("meta") or {}).get(
                "description", ""
            )
            if not isinstance(description, str):
                continue
            tags = [
                tag
                for tag in ("<example>", "<commentary>")
                if tag in description.lower()
            ]
            if tags:
                rel = agent_file.relative_to(REPO_ROOT)
                offenders.append(f"{rel} ({', '.join(tags)})")
>       assert not offenders, (
            "Agent descriptions must contain no <example>/<commentary> blocks "
            "(description-authoring-principles.md V3, #340). Offenders: "
            + "; ".join(offenders)
        )
E       AssertionError: Agent descriptions must contain no <example>/<commentary> blocks (description-authoring-principles.md V3, #340). Offenders: bundles/anchors-amp-dev/agents/amplifier-dev-expert.md (<example>); bundles/anchors-amp-dev/agents/architect.md (<example>); bundles/anchors-amp-dev/agents/builder.md (<example>); bundles/anchors-amp-dev/agents/debugger.md (<example>); bundles/anchors-amp-dev/agents/explorer.md (<example>); bundles/anchors-amp-dev/agents/git-ops.md (<example>); bundles/anchors-amp-dev/agents/researcher.md (<example>)
E       assert not ['bundles/anchors-amp-dev/agents/amplifier-dev-expert.md (<example>)', 'bundles/anchors-amp-dev/agents/architect.md (<example>)', 'bundles/anchors-amp-dev/agents/builder.md (<example>)', 'bundles/anchors-amp-dev/agents/debugger.md (<example>)', 'bundles/anchors-amp-dev/agents/explorer.md (<example>)', 'bundles/anchors-amp-dev/agents/git-ops.md (<example>)', ...]

tests/test_anchors_bundles_dry.py:182: AssertionError
=========================== short test summary info ============================
FAILED tests/test_anchors_bundles_dry.py::TestAmpDevIsAThinVariant::test_declares_no_session_tools_or_hooks
FAILED tests/test_anchors_bundles_dry.py::TestAmpDevIsAThinVariant::test_includes_anchors_by_full_url
FAILED tests/test_anchors_bundles_dry.py::TestNoParallelCopies::test_no_same_path_file_in_both_bundles
FAILED tests/test_anchors_bundles_dry.py::TestNoParallelCopies::test_exactly_one_copy_of_each_ecosystem_doc
FAILED tests/test_anchors_bundles_dry.py::TestAgentDescriptionsCarryNoExampleBlocks::test_no_example_or_commentary_in_any_agent_description
============================== 5 failed in 0.09s ===============================
