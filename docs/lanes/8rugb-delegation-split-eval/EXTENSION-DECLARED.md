# Extension declared BEFORE the runs it buys (2026-09-07T00:13:12Z)

The pre-registered design (n>=3 valid per arm per provider) is COMPLETE: 12 launches, 12 valid,
$62.90 measured. The verdict computed on exactly that pre-registered set is recorded in
ANALYSIS.json (VERDICT = NO-SHIP) and is FINAL; nothing below can change it.

The condition that DECIDED the verdict is the delegation-count condition on the anthropic cell:
arm A delegated [2, 2, 2] and arm B [1, 1, 1] -- zero variance in both arms, ratio 0.50, outside
the frozen +/-30% band. A decision-driving measurement with zero observed variance at n=3 deserves
one falsification attempt, so the residue buys exactly that: ONE more run per arm on the opus
cell (~$9 of the ~$17 remaining).

This is a FALSIFICATION ATTEMPT, not a re-roll:
 * a 4th arm-B opus run recording 2 delegations would weaken the "clean halving" claim;
 * a 4th arm-A opus run recording 1 would do the same from the other side;
 * neither can move the verdict, because the rule is frozen and already applied to the
   pre-registered n=3 set.
Both the pre-registered n=3 table and the extended n=4 table are reported.
