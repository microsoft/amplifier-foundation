bundle:
  name: bundle-design-behavior
  version: 1.0.0
  description: Expert for the full bundle lifecycle -- designing, modeling, and building Amplifier bundles

agents:
  include:
    # Note: agent loading searches /agents dir automatically - don't include "agents" in path
    - foundation:bundle-design-expert

context:
  include:
    # Thin awareness pointer - just enough to know the domain exists and to delegate
    # Heavy documentation lives in the agent file (context sink pattern)
    - foundation:context/bundle-design-awareness.md
